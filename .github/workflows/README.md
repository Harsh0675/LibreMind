# LibreMind CI

Every push to `main` builds the debug APK with GitHub Actions and uploads `LibreMind.apk` as a workflow artifact.

You can also run the workflow manually from the **Actions** tab using **Build LibreMind APK → Run workflow**.
